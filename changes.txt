diff --git a/CMakeLists.txt b/CMakeLists.txt
index 187f085..91d12e4 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -38,6 +38,8 @@ add_executable(${PROJECT_NAME}
 	src/MonsterFactory.h
 	src/TownHall.h
 	src/TownHall.cpp
+	src/Tower.h
+	src/Tower.cpp
 )
 
 # Р»РёРЅСѓРєРµРј РјРѕРґСѓР»Рё SFML
diff --git a/src/GameObject.h b/src/GameObject.h
index 58a77d9..e023f6d 100644
--- a/src/GameObject.h
+++ b/src/GameObject.h
@@ -11,4 +11,5 @@ public:
 	virtual void update(sf::Time deltaTime) = 0;
 	virtual bool isDead() const = 0;
 	virtual sf::FloatRect getBounds() const = 0;
+	virtual void kill() = 0;
 };
\ No newline at end of file
diff --git a/src/GameScene.cpp b/src/GameScene.cpp
index 0af13f1..1426982 100644
--- a/src/GameScene.cpp
+++ b/src/GameScene.cpp
@@ -13,6 +13,11 @@ void GameScene::render(sf::RenderWindow& window)
 
 	if (!townHall.isDead())
 		townHall.render(window);
+
+	for (const auto& tower : towers)
+	{
+		tower->render(window);
+	}
 }
 
 SceneType GameScene::update(sf::Time deltaTime)
@@ -41,10 +46,15 @@ SceneType GameScene::update(sf::Time deltaTime)
 			townHall.takeDamage(1);
 
 			// временное убийство монстра когда он нанес урон
-			dynamic_cast<Monster*>(entity.get())->kill();
+			entity->kill();
 		}
 	}
 
+	for (const auto& tower : towers)
+	{
+		tower->update(deltaTime);
+	}
+
 	// удаляем "мертвых мобов"
 	std::erase_if(entities, [](const std::unique_ptr<GameObject>& obj) { return obj->isDead(); });
 
@@ -61,5 +71,19 @@ SceneType GameScene::processEvent(const sf::Event& event)
 			return SceneType::MainMenu;
 		}
 	}
+
+	// добавление в вектор башен, при нажатии лкм 
+	if (const auto* mousePressed = event.getIf<sf::Event::MouseButtonPressed>())
+	{
+		if (mousePressed->button == sf::Mouse::Button::Left)
+		{
+			float sizeX = 50.0f, sizeY = 50.0f;
+			towers.push_back(std::make_unique<Tower>(
+				sf::Vector2f(sizeX, sizeY),
+				sf::Vector2f( mousePressed->position.x - sizeX / 2, mousePressed->position.y - sizeY / 2), 
+				sf::Color::Green)
+			);
+		}
+	}
 	return SceneType::None;
 }
diff --git a/src/GameScene.h b/src/GameScene.h
index f7710ae..fb3ed6a 100644
--- a/src/GameScene.h
+++ b/src/GameScene.h
@@ -3,6 +3,7 @@
 #include "TownHall.h"
 #include "IScene.h"
 #include "MonsterFactory.h"
+#include "Tower.h"
 
 class GameScene : public IScene
 {
@@ -16,6 +17,7 @@ public:
 
 private:
 	std::vector<std::unique_ptr<GameObject>> entities;
+	std::vector<std::unique_ptr<Tower>> towers;
 	MonsterFactory factory;
 	TownHall townHall = TownHall({ 80.0f, 80.0f }, { 800.0f, 300.0f }, sf::Color::Blue, 5);
 	float spawnTimer = 0.0f;
diff --git a/src/Monster.h b/src/Monster.h
index e8d2203..b31bbca 100644
--- a/src/Monster.h
+++ b/src/Monster.h
@@ -12,8 +12,7 @@ public:
 	void update(sf::Time deltaTime) override;
 	bool isDead() const override;
 	sf::FloatRect getBounds() const override;
-
-	void kill();
+	void kill() override;
 
 private:
 	sf::RectangleShape sprite;
diff --git a/src/Tower.cpp b/src/Tower.cpp
new file mode 100644
index 0000000..5a2be82
--- /dev/null
+++ b/src/Tower.cpp
@@ -0,0 +1,31 @@
+#include "Tower.h"
+
+Tower::Tower(sf::Vector2f size, sf::Vector2f pos, sf::Color color)
+{
+	sprite.setSize(size);
+	sprite.setPosition(pos);
+	sprite.setFillColor(color);
+}
+
+void Tower::render(sf::RenderWindow& window)
+{
+	window.draw(sprite);
+}
+
+void Tower::update(sf::Time deltaTime)
+{
+}
+
+bool Tower::isDead() const
+{
+	return false;
+}
+
+sf::FloatRect Tower::getBounds() const
+{
+	return sprite.getGlobalBounds();
+}
+
+void Tower::kill()
+{
+}
\ No newline at end of file
diff --git a/src/Tower.h b/src/Tower.h
new file mode 100644
index 0000000..ef85b9c
--- /dev/null
+++ b/src/Tower.h
@@ -0,0 +1,18 @@
+#pragma once
+#include "GameObject.h"
+
+class Tower : public GameObject
+{
+public:
+	Tower(sf::Vector2f size, sf::Vector2f pos, sf::Color color);
+	virtual ~Tower() = default;
+
+	void render(sf::RenderWindow& window) override;
+	void update(sf::Time deltaTime) override;
+	bool isDead() const override;
+	sf::FloatRect getBounds() const override;
+	void kill() override;
+
+private:
+	sf::RectangleShape sprite;
+};
\ No newline at end of file
diff --git a/src/TownHall.cpp b/src/TownHall.cpp
index 59a79a0..65975f6 100644
--- a/src/TownHall.cpp
+++ b/src/TownHall.cpp
@@ -36,4 +36,8 @@ void TownHall::takeDamage(int damage)
 int TownHall::getHealth() const
 {
 	return health;
+}
+
+void TownHall::kill()
+{
 }
\ No newline at end of file
diff --git a/src/TownHall.h b/src/TownHall.h
index 4f60b97..bce7764 100644
--- a/src/TownHall.h
+++ b/src/TownHall.h
@@ -11,6 +11,7 @@ public:
 	void update(sf::Time deltaTime) override;
 	bool isDead() const override;
 	sf::FloatRect getBounds() const override;
+	void kill() override;
 
 	void takeDamage(int damage);
 	int getHealth() const;
@@ -18,5 +19,4 @@ public:
 private:
 	sf::RectangleShape sprite;
 	int health;
-	bool dead = false;
 };
\ No newline at end of file
